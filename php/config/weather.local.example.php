<?php
/**
 * Optional local overrides (copy to weather.local.php — gitignored).
 *
 * Open-Meteo works out of the box with no key. Use this only if you self-host
 * Open-Meteo or need a different endpoint.
 */
return [
    // Example: 'https://your-instance.example.com/v1/forecast'
    'open_meteo_forecast_url' => '',
];
